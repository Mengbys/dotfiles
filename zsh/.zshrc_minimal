# Minimal configuration of ZSH
# Under construction!


source ~/.zsh/zsh-z/zsh-z.plugin.zsh

autoload -U colors && colors
autoload -U compinit && compinit

PROMPT="%{$fg[red]%}%c%{$reset_color%} %% "


alias ls="ls --color=tty"
alias ':q'="exit"
